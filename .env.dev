# MapBox access token
# Create one at https://account.mapbox.com/access-tokens/
# Prefix with VITE_ so Vite exposes it to the client
VITE_MAPBOX_TOKEN=

# Server port and bind address
# Set HOST to 0.0.0.0 to allow LAN access, defaults to localhost
PORT=3001
HOST=
